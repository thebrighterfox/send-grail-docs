import{_ as e,o as i,c as n,ag as t}from"./chunks/framework.CvgP6Fyv.js";const f=JSON.parse('{"title":"Fallback & Reliability","description":"","frontmatter":{},"headers":[],"relativePath":"guide/fallback.md","filePath":"guide/fallback.md"}'),s={name:"guide/fallback.md"};function l(o,a,r,c,d,p){return i(),n("div",null,[...a[0]||(a[0]=[t(`<h1 id="fallback-reliability" tabindex="-1">Fallback &amp; Reliability <a class="header-anchor" href="#fallback-reliability" aria-label="Permalink to &quot;Fallback &amp; Reliability&quot;">​</a></h1><p>SendGrail implements a cascading fallback system to maximize email delivery reliability. If the primary SMTP connection fails, SendGrail automatically tries alternative methods before giving up.</p><h2 id="how-the-fallback-cascade-works" tabindex="-1">How the Fallback Cascade Works <a class="header-anchor" href="#how-the-fallback-cascade-works" aria-label="Permalink to &quot;How the Fallback Cascade Works&quot;">​</a></h2><p>When SendGrail attempts to send an email, it follows this sequence:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>1. Primary SMTP Connection (Default Connection)</span></span>
<span class="line"><span>   |</span></span>
<span class="line"><span>   |-- Success -&gt; Email delivered, log result</span></span>
<span class="line"><span>   |-- Failure --v</span></span>
<span class="line"><span>   |</span></span>
<span class="line"><span>2. Fallback SMTP Connection (if configured)</span></span>
<span class="line"><span>   |</span></span>
<span class="line"><span>   |-- Success -&gt; Email delivered, log result</span></span>
<span class="line"><span>   |-- Failure --v</span></span>
<span class="line"><span>   |</span></span>
<span class="line"><span>3. WordPress Default Mail (if enabled in settings)</span></span>
<span class="line"><span>   |</span></span>
<span class="line"><span>   |-- Success -&gt; Email delivered, log result</span></span>
<span class="line"><span>   |-- Failure --v</span></span>
<span class="line"><span>   |</span></span>
<span class="line"><span>4. Log failure, send admin notification (if enabled)</span></span></code></pre></div><p>At each stage, if sending succeeds, the process stops and the result is logged. If it fails, the next method in the cascade is attempted.</p><h2 id="configuring-the-fallback-connection" tabindex="-1">Configuring the Fallback Connection <a class="header-anchor" href="#configuring-the-fallback-connection" aria-label="Permalink to &quot;Configuring the Fallback Connection&quot;">​</a></h2><ol><li>Navigate to <strong>SendGrail &gt; Settings &gt; General</strong>.</li><li>Select a connection from the <strong>Fallback Connection</strong> dropdown.</li><li>Optionally enable <strong>WordPress Default Mail</strong> as a last resort.</li><li>Click <strong>Save Settings</strong>.</li></ol><div class="tip custom-block"><p class="custom-block-title">TIP</p><p>For maximum reliability, use a different SMTP provider for your fallback connection. If your primary provider (e.g., SendGrid) has an outage, a fallback on a different provider (e.g., Gmail) is far more likely to succeed than two connections on the same service.</p></div><h2 id="wordpress-default-mail" tabindex="-1">WordPress Default Mail <a class="header-anchor" href="#wordpress-default-mail" aria-label="Permalink to &quot;WordPress Default Mail&quot;">​</a></h2><p>When the <strong>WordPress Default Mail</strong> toggle is enabled in General settings, SendGrail will attempt to send the email using PHP&#39;s native <code>mail()</code> function as a last resort after both SMTP connections have failed.</p><p>This is not recommended as a primary strategy since <code>mail()</code> has all the deliverability problems that SendGrail exists to solve. However, it can serve as an emergency fallback for critical emails like password resets.</p><div class="warning custom-block"><p class="custom-block-title">WARNING</p><p>WordPress default mail relies on your server&#39;s PHP <code>mail()</code> configuration. On many shared hosting environments, this function is restricted or disabled entirely. Do not rely on it as your only fallback.</p></div><h2 id="failure-notifications" tabindex="-1">Failure Notifications <a class="header-anchor" href="#failure-notifications" aria-label="Permalink to &quot;Failure Notifications&quot;">​</a></h2><p>When an email fails to deliver through all available methods, SendGrail can notify the site administrator. Enable this under <strong>Settings &gt; Notifications &gt; Failure Alerts</strong>.</p><h3 id="preventing-notification-loops" tabindex="-1">Preventing Notification Loops <a class="header-anchor" href="#preventing-notification-loops" aria-label="Permalink to &quot;Preventing Notification Loops&quot;">​</a></h3><p>A natural concern is: what happens if the notification email itself fails? SendGrail handles this with specific safeguards:</p><ul><li>Failure notification emails are sent through the <strong>fallback connection</strong> if one is configured, not the primary connection that just failed.</li><li>If no fallback connection is available, the notification is sent via <strong>WordPress default mail</strong> (<code>mail()</code>), bypassing the SMTP pipeline entirely.</li><li>If the notification email itself fails, the failure is logged but no additional notification is generated. This breaks the potential infinite loop.</li></ul><h2 id="logging-fallback-events" tabindex="-1">Logging Fallback Events <a class="header-anchor" href="#logging-fallback-events" aria-label="Permalink to &quot;Logging Fallback Events&quot;">​</a></h2><p>Every step of the fallback cascade is recorded in the email log. Each log entry includes:</p><ul><li>Which connection was attempted.</li><li>Whether it succeeded or failed.</li><li>The error message (if applicable).</li><li>Which method ultimately delivered the email (or that all methods failed).</li></ul><p>This gives you full visibility into delivery attempts, making it straightforward to diagnose intermittent provider issues.</p><h2 id="best-practices" tabindex="-1">Best Practices <a class="header-anchor" href="#best-practices" aria-label="Permalink to &quot;Best Practices&quot;">​</a></h2><ul><li><strong>Use two different providers.</strong> A fallback on the same provider as your primary does not protect against provider-level outages.</li><li><strong>Test your fallback.</strong> Temporarily set an invalid password on your primary connection and send a test email. Verify that the fallback picks up delivery.</li><li><strong>Monitor failure notifications.</strong> If you start receiving frequent failure alerts, investigate the primary connection rather than relying on the fallback long-term.</li><li><strong>Keep WordPress default mail as a last resort.</strong> Enable it for critical emails, but do not depend on it for reliable delivery.</li></ul>`,24)])])}const g=e(s,[["render",l]]);export{f as __pageData,g as default};
